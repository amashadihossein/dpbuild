# Update exotic column classes of data.frames in a list for sha1 signature

This function is to be used before digest::sha1 to handle exotic column
classes

## Usage

``` r
make_sha1_compatible(l)
```

## Arguments

- l:

  list

## Value

list
