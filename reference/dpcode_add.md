# Adds script templates

Adds dp_make.R and dp_journal.RMD to the dp project repo

## Usage

``` r
dpcode_add(project_path, use_targets = T)
```

## Arguments

- project_path:

  Path to the project

- use_targets:

  T/F when TRUE, uses targets instead of drake (recommended)

## Value

repo
